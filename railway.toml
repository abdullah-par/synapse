[build]
builder = "dockerfile"
dockerfilePath = "backend/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
# We removed startCommand here to allow the Dockerfile CMD to take over,
# which handles $PORT interpolation more reliably in this environment.
