[build]
builder = "nixpacks"

[build.env]
NIXPACKS_BUILD_CMD = "cd backend && pip3 install -r requirements.txt"

[deploy]
startCommand = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"